# Pinned to the version that was installed during scaffolding so
# subsequent setups are reproducible. Bump intentionally when needed.
mcp[cli]==1.27.1
# Live DDB read tools (Phase A2+) use boto3 with the default credential
# chain — picks up the user's `watchtower` SSO cache when Claude Desktop
# launches the server. No keys ever land in this repo.
boto3==1.43.6
# get_planning_context fetches park hours from themeparks.wiki and
# weather from Open-Meteo. requests bundles its own CA cert store
# (certifi), which sidesteps the macOS Python urllib SSL-cert issue
# where urlopen fails with CERTIFICATE_VERIFY_FAILED on a fresh
# Python.org install. boto3 doesn't pull this in transitively.
requests==2.32.5
# ASGI-to-Lambda adapter for the HTTPS transport (mcp/server_http.py).
# FastMCP's streamable_http_app() returns a Starlette ASGI app; Mangum
# wraps it for AWS Lambda + API Gateway. Only used by the HTTPS path —
# the stdio server (server.py) doesn't import it.
mangum==0.18.0
